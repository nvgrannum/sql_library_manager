extends layout

block content
  h1= title
  p Welcome to #{title}

  form(action="/books/new", method="get")
    input(type='submit', value="Create New Book")

  each book in books
    book
      h3= book.title 
      p= book.author  
